# S3 Configuration
S3_ENDPOINT=https://your-s3-endpoint.com
S3_ACCESS_KEY_ID=your-access-key
S3_SECRET_ACCESS_KEY=your-secret-key
S3_BUCKET=your-bucket-name
S3_REGION=us-east-1

# S3 Key Prefix Filter (optional, only index files under this prefix)
# Example: S3_INDEX_PREFIX=documents/transcripts/
S3_INDEX_PREFIX=

# SQLite Database Path (optional, defaults to ./data/search.sqlite)
SQLITE_DB_PATH=./data/search.sqlite

# Job Runner URL (optional, defaults to http://localhost:3001)
JOB_RUNNER_URL=http://localhost:3001

# Authentication
# Set to "true" to disable auth entirely (not recommended)
AUTH_DISABLED=false
# Required when AUTH_DISABLED=false
# Value must be base64("username:bcryptHash")
# Generate with: npm run gen-auth -- <username>
AUTH_CREDENTIAL_B64=

# Sync Configuration
# Enable/disable periodic sync checks (optional, defaults to true)
SYNC_ENABLED=true
# Interval between sync checks in seconds (optional, defaults to 900 = 15 minutes)
SYNC_CHECK_INTERVAL_S=900
